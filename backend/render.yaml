# Render.com deployment configuration for LL&L Futures Backend (Docker)

# Runtime
docker

# Dockerfile Path
backend/Dockerfile

# Environment Variables (set these in Render dashboard)
# PORT=8080
# SPRING_PROFILES_ACTIVE=production

# Docker Build Context
# Root Directory: backend
